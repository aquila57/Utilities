Here is how you run the four analog clocks in this repository.

Time of day clock, clk, has no parameters.

clk

--------

X Windows time of day clock, xclk, has no parameters.

xclk

--------

Stop watch, stopw, has no parameters.

stopw

--------

X Windows stop watch, xstopw, has no parameters.

xstopw

--------

Count down timer, timer, has one parameter,
wait time in seconds.

timer 300

This example sets the count down timer for 5 minutes.

--------

Count down timer, xtimer, has one parameter,
wait time in seconds.

timer 300

This example sets the count down timer for 5 minutes.
